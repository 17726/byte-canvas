# Byte Canvas - 在线无限画布编辑器

基于 Web 的高性能图形编辑器，采用 "渐进式架构" 理念，首个版本（MVP）利用 DOM 的布局与文本编辑优势，构建类似 Figma/ProcessOn 的核心体验。

## 🚀 项目概述

- **核心目标**: 构建一个支持 100-200 图元流畅交互的无限画布编辑器。
- **当前阶段**: 阶段一 (MVP 架构验证)
- **核心能力**:
  - 画布引擎: 无限画布平移 (Pan)、缩放 (Zoom)
  - 基础图元: 矩形、圆形、富文本
  - 基本交互: 单选、拖拽、删除、缩放
  - 数据流: Pinia State -> Canvas Render 单向数据流

## 📅 里程碑 (Milestones)

### 阶段一：MVP 架构验证 (T+5天)

- [ ] 画布平移与缩放
- [ ] 基础图元 (Rect, Circle, Text)
- [ ] 基础交互 (Select, Drag, Delete)
- [ ] 简易工具栏

### 阶段二：基本交付目标 (12月初)

- [ ] 图片图元支持
- [ ] 高级交互 (多选、框选、复制粘贴)
- [ ] 数据持久化 (LocalStorage)
- [ ] 完整工具栏 (侧边 + 悬浮)

### 阶段三：进阶挑战 (12.12 Deadline)

- [ ] 元素旋转
- [ ] 组合/解组 (Group/Ungroup)
- [ ] 性能优化 (100+ 元素 FPS 50+)
- [ ] Undo/Redo

## 🛠 技术栈

- **Framework**: Vue 3 (Composition API)
- **Language**: TypeScript 5.x
- **Build Tool**: Vite
- **State Management**: Pinia
- **Rendering**: DOM + CSS3 Transform (MVP阶段)

## 🏗 系统架构

采用 "四层分层架构" 结合 State/Node 分离模式：

1.  **交互层 (View)**: Vue 组件 (`CanvasEditor.vue`, `Toolbar.vue`)，负责监听用户输入。
2.  **逻辑层 (Controller)**: TS Classes (`BaseNode`, `ToolManager`)，负责坐标转换、碰撞检测。
3.  **数据层 (Model)**: Pinia Store，单一信源，存储扁平化节点树。
4.  **渲染层 (View)**: HTML DOM + CSS3 Transform，响应式渲染。

## 📂 目录结构

```
src/
├── core/                 # [核心层] 纯 TS 逻辑 (Node 类, 工具管理)
│   ├── nodes/            # Node 类定义
│   ├── tools/            # 工具管理器
│   └── utils/            # 数学工具
├── store/                # [数据层] Pinia 状态
├── components/           # [交互/渲染层] Vue 组件
│   ├── editor/           # 画布区域
│   │   ├── CanvasStage.vue
│   │   └── layers/
│   ├── layout/           # 布局 (Toolbar, PropertyPanel)
└── types/                # [类型层] 全局接口定义
```

## 📦 开发指南

### 环境要求

- Node.js >= 20.19.0 或 >= 22.12.0

### 项目设置

```sh
# 安装依赖
npm install
```

### 启动开发服务器

```sh
npm run dev
```

### 类型检查与构建

```sh
# 运行类型检查
npm run type-check

# 构建生产版本
npm run build
```

### 代码规范

```sh
# 运行 Lint 检查
npm run lint

# 格式化代码
npm run format
```

## 📝 协作规范

- **分支管理**: 主干开发模式 (main 受保护)，通过 PR 合并代码。
- **Commit 规范**: 遵循 Conventional Commits (feat, fix, refactor, docs 等)。
- **代码风格**: 启用 ESLint + Prettier，保存自动格式化。
